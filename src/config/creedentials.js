const DB_NAME = "biblioteca";
const DB_USER = "root";
const DB_PASSWORD = "dawnsbalad1234*"
const OBJ_CONN = {
    port: 3306,
    dialect: "mysql",
    host: "localhost"
}

export {
    DB_NAME, DB_USER, DB_PASSWORD, OBJ_CONN
}